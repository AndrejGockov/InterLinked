@model IEnumerable<InterLinked.Models.Application>

@{
    ViewData["Title"] = "My Applications";
}

<h1>My Applications</h1>
<p>View every job, internship and event you've applied to since joining <strong>Linker</strong></p>

<table class="table">
    <thead>
        <tr>
            <th><h4 class="fw-bold">Post</h4></th>
            <th><h5 class="fw-bold">Applied</h5></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @* Use col classes directly on the td, or let the table handle width *@
                <td style="width: 80%;">
                    <div class="mb-1 d-flex align-items-baseline">
                        <h4 class="fw-bold me-2">@item.Post?.Title</h4>
                        <p class="text-muted">@item.Post?.PostedAt.ToString("dd.MM.yyyy")</p>
                    </div>
            
                    <div>
                        <p class="fw-bold fs-6 m-0">Description</p>
                        <p>
                            @(item.Post?.Description?.Length > 80
                                    ? item.Post?.Description.Substring(0, 80) + "..."
                                    : item.Post?.Description)
                        </p>
                    </div>
                </td>

                @* text-center: centers horizontally
               align-middle: centers vertically 
               text-nowrap: forces the date into one line 
            *@
                <td class="text-start align-middle text-nowrap" style="width: 20%;">
                    @item.AppliedAt.ToString("HH:mm | dd.MM.yyyy")
                </td>
            </tr>
        }
    </tbody>

    @* <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <div class="container">
                    <div class="row align-items-center">
                        <td class="col-md-10">
                            @* @item.Post?.Title
                        @item.Post?.Description
                        @item.Post?.User?.UserName

                            <div class="mb-1 d-flex align-items-baseline">
                                <h4 class="fw-bold me-2">@item.Post?.Title </h4>
                                <p class="">@item.Post?.PostedAt.ToString("dd.MM.yyyy")</p>
                            </div>
                        
                            <div>
                                <p class="fw-bold fs-6 m-0">Description</p>
                                <p>
                                    @(item.Post?.Description?.Length > 80
                                            ? item.Post?.Description.Substring(0, 80) + "..."
                                            : item.Post?.Description)
                                </p>
                            </div>
                        </td>

                        <td class="col-md-2 d-flex align-items-center justify-content-center">
                            @item.AppliedAt.ToString("HH:mm | dd.MM.yyyy")
                        </td>
                    </div>
                </div>
            </tr>
        }
    </tbody> *@
</table>